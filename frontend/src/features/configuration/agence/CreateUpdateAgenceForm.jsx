import PropTypes from 'prop-types';
import Form from '../../../ui/Form.jsx';

function CreateUpdateAgenceForm() {
    // Le contenu sera implémenté ultérieurement
    return <Form>Formulaire à implémenter</Form>;
}

CreateUpdateAgenceForm.propTypes = {
    onCloseModal: PropTypes.func,
    agenceToEdit: PropTypes.object,
};

export default CreateUpdateAgenceForm;
